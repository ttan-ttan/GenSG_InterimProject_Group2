# Copy this file to .env and fill in your own values.
# .env is gitignored - never commit real credentials.

# OneMap API access token, from https://www.onemap.gov.sg (Account Settings).
# Tokens expire after 3 days and must be regenerated.
ONEMAP_TOKEN=your_onemap_access_token_here
